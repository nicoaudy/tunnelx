# Domain for subdomains (e.g., yourdomain.com)
DOMAIN=yourdomain.com

# Server port (443 for HTTPS, 80 for HTTP)
SERVER_PORT=443

# Server WebSocket URL (wss:// for secure, ws:// for local)
SERVER_URL=wss://tunnel.yourdomain.com

# Set to true for local testing (HTTP, localhost)
LOCAL=false